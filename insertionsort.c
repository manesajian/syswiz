llist *insertion_sort(llist *lst)
{
    llist *sorted = get_llist();

    while (lst->count > 0) {
        llnode *cur = pop_llist_head(llist *ll);

        for (;;) {

        }
    }

    return sorted;
}
